server:
  port: 8087
  servlet:
    context-path: '/ead-authuser/'
  
spring:
  application:
    name: ead-authuser-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ead-authuser-v2
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non-contextual-creation: true
  output:
    ansi:
      enabled: Always       # mostra o console com letras coloridas
  rabbitmq:
    host: shark.rmq.cloudamqp.com
    port: 5671
    username: dmzqktbw
    password: qYBoCh_nC6gqUTVxiOTgdIwwXFf4-q-m
    virtual-host: dmzqktbw
    ssl:
      enabled: true

ead:
  api:
    url:
      course: 'http://ead-course-service/ead-course'
  broker:
    exchange:
      userEvent: ead.userevent

logging:
  level:
    com.ead: TRACE
    root: INFO
    org.springframework.web: DEBUG   # Caso queira ver o log dessas dependencias
    org.hibernate: INFO
    # org.springframework.amqp: DEBUG
    # org.springframework.amqp.rabbit.connection: DEBUG

eureka:
  client:
    serviceUrl:
      defaultZone: 'http://localhost:8761/eureka'
  instance:
    hostname: localhost
